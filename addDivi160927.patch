位于分支 master
您的分支与上游分支 'origin/master' 一致。

尚未暂存以备提交的变更：
  （使用 "git add <文件>..." 更新要提交的内容）
  （使用 "git checkout -- <文件>..." 丢弃工作区的改动）

	修改:         big_interger_test.c

未跟踪的文件:
  （使用 "git add <文件>..." 以包含要提交的内容）

	addDivi160927.patch
	big_interger_test.c~

修改尚未加入提交（使用 "git add" 和/或 "git commit -a"）
